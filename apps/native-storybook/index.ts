// This app exists solely to host @storybook/react-native for on-device
// verification of @valence/react-native's components — no separate "real
// app" to swap between, so the entry point goes straight to Storybook.
import 'react-native-gesture-handler';
import './.rnstorybook';
